.primary-color-back {
  background-color: #{primaryColor};
}

.accent-color-back {
  background-color: #{accentColor};
}

.accent-color {
  color: #{accentColor};
}

.container-fluid {
  padding-right: 0px;
  padding-left: 0px;
}
